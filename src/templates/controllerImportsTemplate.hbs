{{#if useTypes }} import { Types } from "mongoose"; {{/if}}
import { {{ modelName }} } from '{{ appRef }}/api/models';
import { baseStat } from '{{ appRef }}/api/util/baseStat';
import {
	{{ controllerInterfaceName }},
	{{ controllerInterfaceName }}OutputError,
	{{ controllerInterfaceName }}OutputSuccess
} from '{{ appRef }}/interfaces/api/controllers';
