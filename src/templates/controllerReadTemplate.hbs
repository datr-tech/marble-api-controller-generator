import { {{ modelName }} } from '{{ appRef }}/api/models';
import { {{ controllerInterfaceName }} } from '{{ appRef }}/interfaces/api/controllers';

export const {{ controllerName }}: {{ controllerInterfaceName }} = async ({ {{ modelPrimaryKeyName }} }) => {
  const {{ modelInstanceName }} = await {{ modelName }}.findById({{ modelPrimaryKeyName }});

  return { {{ modelInstanceName }} };
};

