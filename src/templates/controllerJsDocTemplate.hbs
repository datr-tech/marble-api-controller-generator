/**
 * {{ controllerName }}
 *
 * The {{ serviceName }} API {{ methodNameLcase }} {{ baseName }} {{ keywords.controllerLcase }}.
 *
 * @param { {{controllerInterfaceName }}Input } params
 * @param { {{ keywords.typesObjectId }} } params.{{ modelPrimaryKeyName }}
  {{#each fields }} {{#if notPrimaryKey ~}}
* @param { {{ controllerType }} } params.{{ key }} {{#if optionalInController }} (Optional) {{/if }}
  {{/if}} {{/each ~}}
 *
 * @returns { Promise<{{controllerInterfaceName }}Output> }
 * @returns { Promise<{{controllerInterfaceName }}OutputError> } ON ERROR: Promise<{ error: true, payload: { message }}>
 * @returns { Promise<{{controllerInterfaceName }}OutputSuccess> } ON SUCCESS: Promise<{ error: false, payload: { {{#if returnPrimaryKey }} {{~ modelPrimaryKeyName ~}} {{ else }} {{~ modelInstanceName ~}} {{/if }} }}>
 *
 * @author Datr.Tech Admin <admin@datr.tech>
 */ 
