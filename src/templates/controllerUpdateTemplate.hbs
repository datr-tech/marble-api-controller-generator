{{ imports }}

{{ jsdoc }}
export const {{ controllerName }}: {{ controllerInterfaceName }} = async ({ {{ modelPrimaryKeyName }}, payload }) => {

	{{ tryCatchHeader }}
		await {{ modelName }}.findOneAndUpdate(
			{
				_id: {{ modelPrimaryKeyName }}
			},
			payload,
			{
				includeResultMetadata: true
			}
		);

		stat.error = false;
		stat.payload = { {{ modelPrimaryKeyName }} };
		return stat;
	
	{{ tryCatchFooter }}
}

